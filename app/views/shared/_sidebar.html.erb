<br><br>

<div class="collection">
  <%= link_to 'All Products', products_path, class:"collection-item" %>
  <% if Category.any? %>
    <% Category.all.each do |c| %>
      <%= link_to c.name, products_path(category_id: c.id), class:"collection-item" %>
    <% end %>
  <% end %>
</div>

